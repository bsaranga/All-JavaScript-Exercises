// There are 7 primitive types: string, number, bigint, boolean, symbol, null and undefined.

let str = 'Hello World';
let num = Math.PI;

console.log(str.toUpperCase());
console.log(num.toFixed(3)); // sets to 3 dp